export const StatusCall = {
  REST: 0, //không có cuộc gọi
  CALL: 1, //đang gọi đi
  VIDEO_CALL: 2, //đang gọi video đi
  CALL_RECEIVE: 3, //đang nhận cuộc gọi (rung chuông)
  VIDEO_CALL_RECEIVE: 4, //đang nhận cuộc gọi video (rung chuông)
  IN_CALL: 5, //đang trong cuộc gọi
  IN_VIDEO_CALL: 6, //đang trong cuộc gọi video
}
